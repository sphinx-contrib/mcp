on:
  pull_request:

permissions:
  contents: read
  pull-requests: write

name: dco

jobs:
  check:
    name: DCO sign-off
    runs-on: ubuntu-latest

    steps:
      - uses: KineticCafe/actions-dco@5f160e3ee086af8fc0fb567b8add0937d074123e # v3.2.0
